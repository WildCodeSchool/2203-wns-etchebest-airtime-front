export enum ACTION {
  CREATE = "created a new ticket",
  UPDATE = "updated ticket status of",
}

export enum STATUS {
  TODO = "to do.",
  PROGRESS = "to in progress.",
  COMPLETED = "to completed.",
}
